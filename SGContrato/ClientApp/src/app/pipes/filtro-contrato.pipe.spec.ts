import { FiltroContratoPipe } from './filtro-contrato.pipe';

describe('FiltroContratoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroContratoPipe();
    expect(pipe).toBeTruthy();
  });
});
